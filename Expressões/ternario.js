// Operadores Condicional (Ternário)

// Dependendo da condição, nós receberemos valores diferentes.

// Condição então valor 1 se não valor 2 
// condition ? value1 : value2

// ex
// Café da manhã top
 let pao = false
 let queijo = true

 const niceBreakfast = pao || queijo ? 'café toppp' : 'café lixinho'
// Maior de 18

let age = 18

 const canDrive = age >= 18 ? 'canDrive' : "can´t drive" // esse ? é então
console.log(canDrive)
